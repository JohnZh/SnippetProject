<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.john.newtest">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.TestWebActivity" />
        <activity android:name=".activities.DBActivity" />
        <activity android:name=".activities.TestRouteActivity" />
        <activity android:name=".activities.BindLibTestActivity" />
        <activity android:name=".activities.PluginTechActivity" />
        <activity android:name=".activities.HotFixActivity" />
        <activity android:name=".activities.TestAidlActivity" />
        <activity android:name=".viewmodel.ViewModelActivity" />

        <service
            android:name=".service.RemoteService"
            android:exported="true" />

        <activity android:name=".Main4Activity" />
        <activity
            android:name=".Main3Activity"
            android:theme="@style/Theme.AppCompat.Dialog" />

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>